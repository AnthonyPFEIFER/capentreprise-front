export class Game {

    jeuId: number;
    nom: string;
    description: string;
    //dateSortie: Date; // Ou LocalDate ? 
    image: string;
    
    // TODO rajouter les autres propriétés

/*     constructor(nom: string = null, description: string = null, image: string = null ) {
        this.nom = nom;
        this.description = description;
        this.image = image;
    } */
}
